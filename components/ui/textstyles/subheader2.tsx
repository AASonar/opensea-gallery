import React, { FC } from 'react'

const Subheader2:FC = ({children}) => {
  return (
    <div family-name="Work Sans, sans-serif" className="text-xs font-semibold leading-none text-white">{children}</div>
  )
}

export default Subheader2