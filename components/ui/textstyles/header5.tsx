import React, { FC } from 'react'

const Header5:FC = ({children}) => {
  return (
    <div family-name="Work Sans, sans-serif" className="text-lg font-semibold text-white">{children}</div>
  )
}

export default Header5