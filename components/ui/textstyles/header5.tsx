import React, { FC } from "react";

const Header5: FC = ({ children }) => {
  return (
    <div className="font-workSans text-lg font-medium text-white">
      {children}
    </div>
  );
};

export default Header5;
